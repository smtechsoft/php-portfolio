RewriteEngine On

# Pass through existing files and directories
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule . - [L]

# Front controller: route everything else to root index.php
RewriteRule ^.*$ index.php [L,QSA]